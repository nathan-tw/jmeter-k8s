stages:
  - invoke


invoke:
  stage: invoke
  script:
    - sh ./start_test.sh test-plan.jmx $TARGET_HOST $TARGET_PATH $TARGET_PORT $TARGET_PROTOCOL $TARGET_LOOPS $THREADS $influxdbUrl 
  tags:
    - public