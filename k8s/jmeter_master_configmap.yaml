apiVersion: v1
kind: ConfigMap
metadata:
  namespace: jmeter
  name: jmeter-load-test
  labels:
    app: influxdb-jmeter
data:
  load_test: |
    #!/bin/bash

    /jmeter/apache-jmeter-*/bin/jmeter -n -Dlog_level.jmeter=DEBUG -t $1 -R`getent ahostsv4 jmeter-slaves-svc | cut -d' ' -f1 | sort -u | awk -v ORS=, '{print $1}' | sed 's/,$//'` \
    -GTARGET_HOST=$2 -GTARGET_PATH=$3 -GTARGET_PORT=$4 \
    -GTARGET_PROTOCOL=$5  \
    -GTARGET_LOOPS=$6 -GTHREADS=$7 \
    -GinfluxdbUrl=$8 -GinfluxdbToken=$9 

    cat jmeter.log